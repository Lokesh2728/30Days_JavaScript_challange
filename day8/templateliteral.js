// template literals
// template literals are a new way to write strings in JavaScript. They are a way to write strings
// that are easier to read and write. They are also a way to write strings that are more
// powerful and flexible. 
const name="Lokesh";
let age=22;
const result=(`username is ${name} and age is${age}`)
console.log(result)

// template literalmultiline string
const multilineString = `
This is a multiline string
that spans across multiple lines
and is very convenient to use
`;

console.log(multilineString);